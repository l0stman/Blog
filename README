This is a simple blog engine on top of Hunchentoot.  The blog is saved
directly to disk using a flat file.

After loading successfully the package, visit the uri ``/blog''.  To
log in, admin is both the default username and password.
